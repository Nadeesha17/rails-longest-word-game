<div class="container text-center mt-5">
  <h1> Longest Word Game</h1>
  <p>Use the following letters to form a valid word:</p>

  <h2 class="text-primary"><%= @letters.join(" ") %></h2>

  <form action="<%= score_path %>" method="post" data-turbo="false">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag :letters, @letters.join(" ") %>

    <input type="text" name="word" class="form-control my-3" placeholder="Enter a word" required>
    <button type="submit" class="btn btn-success">Submit</button>
  </form>
</div>
